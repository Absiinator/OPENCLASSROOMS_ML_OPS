"""
Setup script for Home Credit Scoring project.
"""

from setuptools import setup, find_packages

if __name__ == "__main__":
    setup(
        name="home-credit-scoring",
        version="1.0.0",
        packages=find_packages(),
        python_requires=">=3.10",
    )
