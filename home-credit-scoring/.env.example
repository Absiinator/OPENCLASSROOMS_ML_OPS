# ============================================
# Variables d'environnement - Home Credit Scoring
# ============================================
# Copiez ce fichier en .env et adaptez les valeurs

# ============================================
# Ports par défaut (configuration locale)
# ============================================
# API FastAPI        : 8000
# Dashboard Streamlit: 8501
# MLflow UI          : 5002 (5000/5001 utilisés par AirPlay sur macOS)

# ============================================
# API Configuration
# ============================================
PORT=8000
HOST=0.0.0.0

# Chemins des modèles (relatifs ou absolus)
MODEL_PATH=./models/lgbm_model.joblib
PREPROCESSOR_PATH=./models/preprocessor.joblib
CONFIG_PATH=./models/model_config.json

# Seuil de décision (optionnel, lu depuis model_config.json par défaut)
# THRESHOLD=0.44

# ============================================
# Dashboard Configuration
# ============================================
# URL de l'API pour le dashboard Streamlit
# En local:
API_URL=http://localhost:8000
# En production (Render):
# API_URL=https://home-credit-api.onrender.com

# Port du dashboard (local)
STREAMLIT_PORT=8501

# ============================================
# MLflow Configuration
# ============================================
MLFLOW_TRACKING_URI=./notebooks/mlruns
MLFLOW_EXPERIMENT_NAME=home-credit-scoring
MLFLOW_MODEL_NAME=home_credit_model
MLFLOW_PORT=5002

# ============================================
# Données (optionnel)
# ============================================
# Chemin vers les données si différent du défaut
# DATA_DIR=./data

# ============================================
# Production - Render (gratuit)
# ============================================
# Ces variables sont définies dans le dashboard Render
# PYTHONPATH=/app
# PORT=8000  (API) ou PORT=8501 (Dashboard)
# API_URL=https://home-credit-api.onrender.com

# ============================================
# Kaggle API (pour télécharger les données)
# ============================================
# KAGGLE_USERNAME=votre_username
# KAGGLE_KEY=votre_api_key
